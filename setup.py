from setuptools import setup, find_packages

setup(
    name='data_loader',
    version='0.1',
    author='Daniel Meissner',
    packages=find_packages()    # It looks for directories containing "__init__.py"
)